import 'package:flutter/material.dart';

class BackStats extends StatelessWidget {
  const BackStats({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
